<!DOCTYPE html>
<html>
  <head>
    <title>Sys-Venus</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

 <body>

    <div id="wrapper">

        <!-- Navigation -->
        <nav class="navbar navbar-default navbar-static-top" role="navigation" style="margin-bottom: 0">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <%= link_to 'Peluqueria Venus', root_path , class: 'navbar-brand' %>
           </div>
            <!-- /.navbar-header -->

            <ul class="nav navbar-top-links navbar-right">

                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                        <% if user_signed_in?%>
                        <i class="fa fa-user fa-fw"></i>Usuario: <%= current_user.nombre%></i><i class="fa fa-caret-down"></i>
                        <% end %>
                    </a>
                    <ul class="dropdown-menu dropdown-user">
                    <% if user_signed_in?%>
                        <li>
                        <%= link_to "Cerrar sesión",destroy_user_session_path,method: :delete,class:''%>
                        </li>
                        <% else%>
                        <%= link_to "Inicio Sesion",new_user_session_path%>
                        <%= link_to "Registro Sesion",new_user_registration_path%>
                        <% end %>
                    </ul>
                    <!-- /.dropdown-user -->
                </li>
                <!-- /.dropdown -->
            </ul>
            <!-- /.navbar-top-links -->

        <%= render 'layouts/menu' %>
            <!-- /.navbar-static-side -->
        </nav>

        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header"><%=  %></h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->

            <div class="row">
                <div class="col-lg-12">
                <%= yield %>
                </div>
            </div>
      
        <!-- /#page-wrapper -->

    </div>
    <!-- /#wrapper -->
</body>

</html>
